# Never commit game-derived assets (copyrighted) or build outputs
*.scd
*.dds
*.bdf
*.bin
*.png

# Python
__pycache__/
*.pyc

# Local working dirs
stage*/
base*/
navwork/
previews/
archive/

# Session artifacts (never publish)
_tg/
*.log
_legacy_nav_out.txt

# Secrets safety net — sc2bot config lives OUTSIDE the repo (see CLAUDE.md);
# these patterns are insurance against a stray copy ever being staged
telegram.json
state.json
config/
